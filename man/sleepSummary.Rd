% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sleep_process.R
\name{sleepSummary}
\alias{sleepSummary}
\title{Function to processes sleep runs and extract summary values.}
\usage{
sleepSummary(
  df,
  sleep_var,
  time_var,
  epoch_length_min,
  min_observed_hours = 18
)
}
\arguments{
\item{df}{Dataframe with a vector of times (POSIXct format) and sleep/wake states (0 = wake, 1 = sleep).}

\item{sleep_var}{String - name of sleep variable in df dataframe}

\item{time_var}{String - name of time variable in df dataframe}

\item{epoch_length_min}{Numeric value of the length of each epoch in minutes.}

\item{min_observed_hours}{Minimum hours of data observed for the day, based on
epoch_length_min, required for a day to be considered valid for the calculation
of sleep statistics. Default is 18 hours.}
}
\value{
A list with several summaries. The first is a dataframe with following
summary values for all sleep runs (excluding those that hit the beginning or end
of the data): Times of sleep onsets, times of sleep midpoints, times of sleep offsets,
durations of sleep (in original time scale), start indices of each sleep run,
and end indices for each sleep run. Dataframes are also returned for
24-hour summaries for both noon-to-noon and midnight-to-midnight values.
}
\description{
Function to processes sleep runs and extract summary values.
}
